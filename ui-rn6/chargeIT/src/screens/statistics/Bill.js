import { View, Text, StyleSheet } from "react-native";

const Bill = () => {
    return (
        <View>
            <Text>Test</Text>
        </View>
    );
};

const styles = StyleSheet.create({
});

export default Bill;
